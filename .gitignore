node_modules/
dist/
.env
.env.*
!.env.example
coverage/
*.log
.DS_Store

# direnv (local env, may hold DB creds / secrets)
.envrc

# Sim-fleet plaintext bearer keys (#248) — provisioned locally, NEVER commit.
sim-credentials.local.json
